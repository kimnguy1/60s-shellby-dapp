# Crypto Airdrop Studio

Baseline DevOps scaffolding for Shelby dapp MVP:
- Reproducible local runtime via DevContainer + Docker Compose
- CI pipeline (`lint`, `test`, `build`)
- CD staging workflow and production placeholder
- Environment/secrets policy and deploy/rollback runbook

## Quick start
```bash
bash scripts/setup-local.sh
```
